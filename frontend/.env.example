# Public — baked into the client bundle at build time. The browser uses this to
# call the API directly. For docker-compose default port mapping, this is the
# host-side URL (the browser is on the host, not inside the compose network).
NEXT_PUBLIC_API_URL=http://localhost:8000
