/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { PullCaptureRelayCaptureSessionsSessionIdRelayGetOptions, PullCaptureRelayCaptureSessionsSessionIdRelayGetResponses } from '../types/PullCaptureRelayCaptureSessionsSessionIdRelayGet'
import { client } from '../.kubb/client'

/**
 * @description Pull the parked rubber_stamp photo into the desktop form, one-shot.
 * Clears the relay slot on read so a re-poll won't re-deliver the same
 * photo; the desktop loads the bytes into the stamp editor and then
 * closes the session.
 * @summary Pull Capture Relay
 * {@link /capture/sessions/:session_id/relay}
 */
export function pullCaptureRelayCaptureSessionsSessionIdRelayGet<ThrowOnError extends boolean = true>(options: Options<PullCaptureRelayCaptureSessionsSessionIdRelayGetOptions, ThrowOnError>): Promise<RequestResult<PullCaptureRelayCaptureSessionsSessionIdRelayGetResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'GET', url: '/capture/sessions/{session_id}/relay', security: [{ type: 'apiKey', name: 'session', in: 'cookie' }], ...config }) as Promise<RequestResult<PullCaptureRelayCaptureSessionsSessionIdRelayGetResponses, ThrowOnError>>
}
